# Normalise line endings so a Windows checkout does not produce a CRLF diff for
# every file in the repository.
* text=auto eol=lf

# Binary assets: never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.woff binary
*.woff2 binary
